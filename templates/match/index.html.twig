{% extends 'base.html.twig' %}

{% block title %}Matchs du tournoi{% endblock %}

{% block body %}
<section class="match-list-section">
    <h1 class="page-title">🏆 Matchs du tournoi : {{ tournament.name }}</h1>
    <p class="tournament-info">📍 {{ tournament.location }} | 🗓 {{ tournament.date|date('d/m/Y H:i') }}</p>

    {% if matches is empty %}
        <p class="empty-text">Aucun match n’a encore été enregistré pour ce tournoi.</p>
    {% else %}

        {# ===================== MATCHS EN COURS ===================== #}
        <h2 class="section-title">🕹️ Matchs en cours</h2>
        <div class="matches-grid">
            {% set hasActive = false %}
            {% for match in matches %}
                {% if match.score1 > 0 and match.score2 > 0 %}
                    {% set hasActive = true %}
                    <div class="match-card">
                        <div class="player-card player-left">
                            <div class="avatar neon-avatar-blue">
                                <img src="{{ asset('img/avatars/' ~ match.player1.avatar|default('default.png')) }}" alt="Avatar joueur 1">
                            </div>
                            <h3>{{ match.player1.userIdentifier }}</h3>
                            <p class="level">NIV {{ match.player1.level|default(1) }}</p>
                            <p class="score">{{ match.score1 }} HP</p>
                        </div>

                        <div class="vs-circle">VS</div>

                        <div class="player-card player-right">
                            <div class="avatar neon-avatar-pink">
                                <img src="{{ asset('img/avatars/' ~ match.player2.avatar|default('default.png')) }}" alt="Avatar joueur 2">
                            </div>
                            <h3>{{ match.player2.userIdentifier }}</h3>
                            <p class="level">NIV {{ match.player2.level|default(1) }}</p>
                            <p class="score">{{ match.score2 }} HP</p>
                        </div>

                        <div class="match-footer">
                            <p class="status text-info">En cours</p>
                            <a href="{{ path('app_tournament_match_show', {tournamentId: tournament.id, id: match.id}) }}" class="btn-view">Voir le match</a>
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
            {% if not hasActive %}
                <p class="empty-text">Aucun match en cours.</p>
            {% endif %}
        </div>

        <hr class="section-separator">

        {# ===================== MATCHS TERMINÉS ===================== #}
        <h2 class="section-title">🏁 Matchs terminés</h2>
        <div class="matches-grid">
            {% set hasFinished = false %}
            {% for match in matches %}
                {% if match.score1 == 0 or match.score2 == 0 %}
                    {% set hasFinished = true %}
                    {% set winner = match.score1 > match.score2 ? match.player1 : match.player2 %}
                    {% set loser = match.score1 > match.score2 ? match.player2 : match.player1 %}

                    <div class="match-card match-finished">
                        <div class="player-card player-left {% if winner.id == match.player1.id %}winner{% else %}loser{% endif %}">
                            <div class="avatar {% if winner.id == match.player1.id %}neon-avatar-blue{% else %}avatar-gray{% endif %}">
                                <img src="{{ asset('img/avatars/' ~ match.player1.avatar|default('default.png')) }}" alt="Avatar joueur 1">
                            </div>
                            <h3>{{ match.player1.userIdentifier }}</h3>
                            <p class="result-label">
                                {% if winner.id == match.player1.id %}
                                    🏆 Gagnant
                                {% else %}
                                    ❌ Perdant
                                {% endif %}
                            </p>
                            <p class="score">{{ match.score1 }} HP</p>
                        </div>

                        <div class="vs-circle">VS</div>

                        <div class="player-card player-right {% if winner.id == match.player2.id %}winner{% else %}loser{% endif %}">
                            <div class="avatar {% if winner.id == match.player2.id %}neon-avatar-pink{% else %}avatar-gray{% endif %}">
                                <img src="{{ asset('img/avatars/' ~ match.player2.avatar|default('default.png')) }}" alt="Avatar joueur 2">
                            </div>
                            <h3>{{ match.player2.userIdentifier }}</h3>
                            <p class="result-label">
                                {% if winner.id == match.player2.id %}
                                    🏆 Gagnant
                                {% else %}
                                    ❌ Perdant
                                {% endif %}
                            </p>
                            <p class="score">{{ match.score2 }} HP</p>
                        </div>

                        <div class="match-footer">
                            <p class="status text-success">Terminé</p>
                            <a href="{{ path('app_tournament_match_show', {tournamentId: tournament.id, id: match.id}) }}" class="btn-view">Détails</a>
                        </div>
                    </div>
                {% endif %}
            {% endfor %}
            {% if not hasFinished %}
                <p class="empty-text">Aucun match terminé.</p>
            {% endif %}
        </div>
    {% endif %}
</section>
{% endblock %}
