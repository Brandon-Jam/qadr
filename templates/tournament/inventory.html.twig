{% extends 'base.html.twig' %}

{% block body %}
<section class="inventory-section">
    <h1 class="inventory-title">🎴 Inventaire – {{ tournament.name }}</h1>
    <p class="credits">💰 Crédits restants : <strong>{{ participant.credits }}</strong></p>

    {% if cards is empty %}
        <p class="empty-text">Aucune carte achetée pour ce tournoi.</p>
    {% else %}
        <div class="inventory-grid">
            {% for item in cards %}
                <div class="inventory-card rarity-{{ item.card.rarity|default('normal')|lower }}">
                    {% if item.card.image %}
                        <img src="{{ asset('img/cards/' ~ item.card.image) }}" alt="{{ item.card.name }}">
                    {% endif %}
                    <h3 class="card-name">{{ item.card.name }}</h3>
                    <p class="card-effect">{{ item.card.effect }}</p>
                    <div class="card-quantity">x{{ item.quantity }}</div>
                </div>
            {% endfor %}
        </div>
    {% endif %}

    <a href="{{ path('app_tournament_show', {id: tournament.id}) }}" class="btn-back">← Retour au tournoi</a>
</section>
{% endblock %}